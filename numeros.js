function myFunction(numeros) {

    numeros = document.querySelector("#numeros").value;
    for (let index = numeros; index > 0; index--) {
        document.writeln(index);   
             
    }
  }